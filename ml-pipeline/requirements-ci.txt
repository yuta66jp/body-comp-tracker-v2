# Minimal dependencies for running ml-pipeline unit tests in CI.
# Does NOT include neuralprophet / torch (too heavy for CI install).
# Used by .github/workflows/ci.yml python-test job.
#
# 依存の整理状況:
#   analyze.py: xgboost は run_importance() 内で遅延 import、
#               supabase は main() 内で遅延 import 済み。
#               → import analyze だけでは xgboost / supabase 不要になった。
#   enrich.py:  supabase をトップレベル import しているため、
#               test_enrich.py の import 時に supabase が必要（未整理）。
#               → supabase はここに残す。
#               除去条件: enrich.py の `from supabase import create_client` を
#               main() 内への遅延 import に移動すれば、この行を削除できる。
pandas==2.*
xgboost==2.*
scikit-learn>=1.3
supabase==2.*
pytest
